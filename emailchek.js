// function validmail(){
//    const usermail=document.querySelector("#email").value;
//    const emailp=/([a-zA-Z0-9._-]+@[a-zA-Z0-9._-]+\.[a-zA-Z0-9._-]+)/g;

//    if(emailp.test(usermail)){
//     alert("your mailid is valid")
    
//    }
//    else{
//     alert("your mailid is not valid")
//    }
// }

// document.getElementById("search-button").addEventListener("click", function() {
//     const searchTerm = document.getElementById("search-input").value;
//     const regex = new RegExp(searchTerm, "gi");
//     const contentDiv = document.getElementById("content");
//     const paragraphs = contentDiv.getElementsByTagName("p");

//     for (let paragraph of paragraphs) {
//         paragraph.innerHTML = paragraph.textContent.replace(regex, (match) => `<mark>${match}</mark>`);
//     }
// });
